* README-OBSOLETE.org in org-media-notes/contrib

This contains references to files still around in this contrib
directory which are not usable with current org-media-note.

#+begin_src elisp
;; an alternative to pretty-hydra (only uses regular hydra)
(require 'omn-hydra)

;; or use the so-called "transient" package
(require 'omn-transient)

;; this is now in org-ref (commit 2aa6dd687 2023-06-29)
(require 'org-ref-publish)

;; the following were provided as so called "transient-ui"
;; replacements for hydra functions in org-ref.
;; as of org-ref commit 4500ddd 2025-10-12 there are "transient"
;; replacements in org-ref
(require 'or-transient)
(require 'orb-transient)
(require 'orc-transient)
#+end_src

* Renamed functions
#+begin_src elisp
;; org-ref commit 55951f4ed  (2024-08-09)
(defun org-media-note-get-media-file-by-key (key) (org-media-note-cite--file-path key))
(defun org-media-note-get-url-by-key (key) (org-media-note-cite--url key))
(defun org-media-note-media-cite-link-follow (key) (org-media-note-cite--open key))
#+end_src